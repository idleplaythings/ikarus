#!/usr/bin/env node
"use strict";

var path = require("path");
var main = require("./../lib/client-main.js");
var yargs = require("yargs");

var argv = yargs
    .usage(main.usage)
    .options(main.options)
    .help("help")
    .alias("help", "h")
    .argv;


main(argv);

